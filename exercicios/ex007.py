# Calcular a media de um valor
n1 = float(input('Digite a primeira nota: '))
n2 = float(input('Digite outra nota: '))
s = n1 + n2
m = (n1 + n2)/2
print('A media entre {:.1f} e {:.1f} é iguala a {:.1f}'.format(n1, n2, m))
#print('A media da nota é {}'.format(s/2))
